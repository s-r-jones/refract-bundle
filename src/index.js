import React from 'react'
import ReactDOM from 'react-dom/client'
import Refract from './components/refract';

ReactDOM.createRoot(document.getElementById('three-root')).render(<Refract />,)

// import('./components/refract').then(({ default: App }) => {
//   ReactDOM.createRoot(document.getElementById('three-root')).render(<App />,);
// })


